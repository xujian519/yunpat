[package]
name = "vector-service"
version = "0.0.1"
edition = "2021"

[dependencies]
tokio = { version = "1.35", features = ["full"] }
tonic = "0.11"
prost = "0.12"
async-trait = "0.1"

[build-dependencies]
tonic-build = "0.11"
