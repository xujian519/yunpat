[workspace]
members = [
    "yunpat-tokenizer",
    "yunpat-similarity",
    "tools-service",
]
resolver = "2"

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
